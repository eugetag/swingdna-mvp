import type { ReactNode } from "react";

export const metadata = {
  title: "Sign up | SwingDNA",
  description: "Create your SwingDNA account.",
};

export default function SignupLayout({ children }: { children: ReactNode }) {
  return children;
}
